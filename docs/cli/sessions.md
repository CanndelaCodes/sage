---
summary: "CLI reference for `sage sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `sage sessions`

List stored conversation sessions.

```bash
sage sessions
sage sessions --active 120
sage sessions --json
```
