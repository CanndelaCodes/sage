---
summary: "CLI reference for `sage uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `sage uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
sage uninstall
sage uninstall --all --yes
sage uninstall --dry-run
```
